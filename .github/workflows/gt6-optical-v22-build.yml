name: GT6 Optical Lens Test v2.2 APK

on:
  push:
    branches: [ "gt6-optical-v22-build" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK 35
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"
      - name: Unpack GT6 v2.2 project
        run: |
          rm -rf /tmp/gt6-v22
          mkdir -p /tmp/gt6-v22
          unzip -q gt6-optical/GT6OpticalLensTest-v2.2-ROLE-FPS-SOURCE.zip -d /tmp/gt6-v22
      - name: Build debug APK
        working-directory: /tmp/gt6-v22/GT6OpticalLensTest
        run: gradle --no-daemon --stacktrace :app:assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: GT6OpticalLensTest-v2.2-debug
          path: /tmp/gt6-v22/GT6OpticalLensTest/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
